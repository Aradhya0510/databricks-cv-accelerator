# Local Development Dependencies
# For Databricks runtime extras (packages needed on-cluster but not in DBR ML),
# see requirements_runtime.txt — those are auto-installed by jobs/*.py entry points.

# --- Runtime deps (also listed in requirements_runtime.txt) ---
pycocotools>=2.0.6  # COCO dataset handling (detection task)

# --- Testing ---
pytest>=7.3.1
pytest-cov>=4.1.0

# --- Documentation ---
sphinx>=6.1.3
sphinx-rtd-theme>=1.2.0

# Note: The following are provided by DBR ML runtime and don't need to be installed:
# - torch, torchvision, torchmetrics
# - transformers, tokenizers
# - numpy, pandas, pillow, requests
# - pyyaml, tqdm, opencv-python
