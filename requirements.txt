# Local Development Dependencies
# Core runtime deps (auto-installed on Databricks by jobs/*.py entry points):
-r requirements_runtime.txt

# --- Testing ---
pytest>=7.3.1
pytest-cov>=4.1.0

# --- Documentation ---
sphinx>=6.1.3
sphinx-rtd-theme>=1.2.0

# Note: The following are provided by DBR ML runtime and don't need to be installed:
# - torch, torchvision, torchmetrics
# - transformers, tokenizers
# - numpy, pandas, pillow, requests
# - pyyaml, tqdm, opencv-python
